# Git Workflow Rule
Do not create a separate branch or open a Pull Request when modifying code. Since the user is the repository owner and admin, all code changes should be committed and pushed directly to the `main` branch.
